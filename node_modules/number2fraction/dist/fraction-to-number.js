"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.fraction2number = void 0;
const fractionPattern = /^\d+\/\d+$/;
function fraction2number(f) {
    if (!fractionPattern.test(f)) {
        throw new Error('input is not a fraction');
    }
    const fractions = f.split('/');
    if (!(fractions === null || fractions === void 0 ? void 0 : fractions.length) || (fractions === null || fractions === void 0 ? void 0 : fractions.length) < 2) {
        return 0.0;
    }
    const numerator = parseFloat(fractions[0] || '0');
    const denominator = parseFloat(fractions[1] || '1');
    return numerator / denominator;
}
exports.fraction2number = fraction2number;
